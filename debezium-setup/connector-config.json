{
  "name": "postgres_connector",
  "config": {
    "connector.class": "io.debezium.connector.postgresql.PostgresConnector",
    "database.hostname": "${file:/secrets/postgres.properties:hostname}",
    "database.port": "${file:/secrets/postgres.properties:port}",
    "database.user": "${file:/secrets/postgres.properties:user}",
    "database.dbname": "${file:/secrets/postgres.properties:db}",
    "database.password": "${file:/secrets/postgres.properties:password}",
    "database.server.name": "fullfillment",
    "table.include.list": "new_schema.resources",
    "publication.name": "my_publication",
    "topic.prefix": "resources",
    "plugin.name": "pgoutput",
    "transforms": "Reroute,ExtractNewRecordState",
    "transforms.Reroute.type": "io.debezium.transforms.ByLogicalTableRouter",
    "transforms.Reroute.topic.regex": "new_schema.resources",
    "transforms.Reroute.topic.replacement": "repos-topic",
    "transforms.ExtractNewRecordState.type": "io.debezium.transforms.ExtractNewRecordState",
    "transforms.ExtractNewRecordState.drop.tombstones": "true",
    "transforms.ExtractNewRecordState.delete.handling.mode": "drop"
  }
}
